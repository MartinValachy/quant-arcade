# 19 — Spot the Bias

## Claim to verify

The displayed sequence is generated by the claimed Bernoulli bias model and the
classification answer is scored against the correct hypothesis.

## Current finding

The plan reports the core as correct but requires statistical confirmation.

## Check

Run at least 10,000 independent sequences, compare empirical means/variances to the
Bernoulli/binomial model, and inspect equal-bias, short-sample, and tail cases.

## Completed evidence — 2026-09-17

Status: ✅ clean. The actual round generator was checked over 20,000 rounds per
variant. Target indices were uniform, exactly one coin had a non-0.5 probability,
the sign was balanced, and absolute bias stayed within the configured interval.

For both positive and negative endpoint probabilities, 20,000 Bernoulli flips per
case matched the theoretical mean/variance and an independent NumPy check. The
classification path uses the generated target index directly and produced no
ambiguous or invalid target state.

Evidence commands:

```text
node verification/node/19_biased_coin_check.js
PYTHONPATH=verification python verification/python/tier2_stats/19_biased_coin_checks.py
```
