# 03 — Conditional Traps

## Claim to verify

The displayed conditional probability matches the mechanism and conditioning event
actually generated by each of the seven question families.

## Current finding

All seven probability mechanisms are correct. A presentation defect was found
in `build`: small true probabilities could leave the random distractor filler
with only two or three choices after its retry guard. A deterministic fallback
grid now guarantees four distinct answers without changing the correct value.

## Check

`verification/node/03_cond_prob_check.js` called every shipped `FAM` family
10,000 times, including generalized Monty Hall and retrodiction. The Python
check independently re-derived all answers with exact fractions; all 70,000
cases matched. It also generated 10,000 standard and 10,000 hard build cards,
confirming four answer choices on every card.

## Expected outcome

🔧 verified; exact fractions and answer-set invariants passed after the filler
fallback fix.
